# MultiFormer 论文复现计划（基于 MM-Fi 数据集）

> 论文：*MultiFormer: A Multi-Person Pose Estimation System Based on CSI and Attention Mechanism*（Qu et al., arXiv:2505.22555v2, 2025）
> 工作目录：`C:\Users\杜迪安\Desktop\鸿鹄实验参考论文复现\`
> 数据集：`D:\MM-Fi数据集\MMFi_Dataset\`

---

## 1. Context（背景与目标）

复现 MultiFormer 系统的"学生网络"，用 WiFi CSI 数据预测人体 2D 姿态。论文核心创新有 3 点：
1. **TFDDT**（Time-Frequency Dual-Dimensional Tokenization）：把 CSI 上采样后分别按"频率"和"时间"两个维度切分成 token 序列。
2. **双路 Multi-Head Self-Attention 特征提取器**：两路并行的 Transformer Encoder 分别处理时间和频率 token。
3. **MSFN**（Multi-Stage Feature Fusion Network）：3 个迭代细化的 Heatmap Decoder + PAPM（通道+空间注意力）模块，输出 PCM/PAF 热图。

**与论文的差异点（必须改造的部分）：**

| 项目 | 论文（自采集 + Intel 5300） | MM-Fi（本次使用） |
|---|---|---|
| 接收天线 NR | 3 | 3 ✓ 一致 |
| 子载波数 NS | 30 → 上采样到 64 | 114 → **保留 114（不下采样）** |
| 数据包数 M | 10 → 上采样到 64 | 10 → **上采样到 64** ✓ 一致 |
| 关键点数 | 18（OpenPose，含 neck） | 17（COCO） → **加 neck = (L_sh+R_sh)/2 → 18** |
| 教师网络（PCM/PAF GT） | OpenPose 在线推理 | 离线用 `pose2d.npy` **直接合成 PCM/PAF GT** |
| 数据集 | 自采集 + MM-Fi | 仅 MM-Fi |

教师网络部分可以省略：MM-Fi 已经提供 17-keypoint 的 2D 标注（`pose2d.npy`，由 RGB+OpenPose 离线生成），直接用它构造 PCM/PAF Ground Truth 即可。

**关于子载波数为何不下采样到 64：**
> 论文是把 30 个子载波**上采样**到 64（信息扩展，不丢失）；MM-Fi 已有 114 个子载波，下采样到 64 会经过 FFT 抗混叠低通滤波，**丢掉约 44% 的频域分辨率**——尤其影响那些对手腕/肘部高动态关键点敏感的小尺度多径变化。因此本复现保留全部 114 子载波，模型按 NS=114 适配（frequency token 数变成 114，token 长度仍为 M·NR=192）。
> 备选：若想严格保持论文 64×64 对称结构，可以用一个**可学习的 1×1 conv（114→64 在 subcarrier 维）**替代 FFT resample，参数极少且让网络自己学哪些子载波重要。本计划默认走"保留 114"方案，备选方案在 config 里加开关。

---

## 2. 项目结构

```
鸿鹄实验参考论文复现/
├── 参考论文/                          # 已存在
├── plan.md                           # 本计划（拷贝自 ~/.claude/plans/...）
├── configs/
│   └── default.yaml                  # 训练超参、数据路径
├── data/
│   ├── __init__.py
│   ├── mmfi_dataset.py               # MM-Fi Dataset 类
│   ├── csi_preprocess.py             # CSI 上采样/重采样、TFDDT 预处理
│   └── heatmap_gt.py                 # 由 pose2d 合成 PCM + PAF
├── models/
│   ├── __init__.py
│   ├── tfddt.py                      # Token 化模块（含可学习 embedding）
│   ├── attention_extractor.py        # 双路 MHSA + Reconstruction Layer
│   ├── papm.py                       # 通道+空间注意力
│   ├── heatmap_decoder.py            # 7-conv 解码器输出 PCM/PAF
│   ├── msfn.py                       # 3-stage MSFN
│   └── multiformer.py                # 顶层模型
├── decode/
│   └── pose_decoder.py               # NMS + PAF 积分 + 匈牙利匹配
├── utils/
│   ├── metrics.py                    # PCK@k
│   └── viz.py                        # 骨架绘制工具（GT vs 预测）
├── train.py                          # 训练入口
├── eval.py                           # 评估入口
├── visualize.py                      # 每个环境随机采 N 帧，输出 GT/Pred 对比图
├── outputs/
│   └── viz/                          # 可视化结果输出（按 env 分目录）
│       ├── E01/
│       ├── E02/
│       ├── E03/
│       └── E04/
├── requirements.txt
└── README.md
```

---

## 3. 关键代码思路

### 3.1 数据加载 `data/mmfi_dataset.py`

**遍历目录**：`MMFi_Dataset/E{01-04}/S{01-..}/A{01-27}/`，每个动作目录是一个 trial。

**单 sample 构造**：以"frame index t"为索引：
- 读 `wifi-csi/frame{t:03d}.mat` → `CSIamp` shape `(3, 114, 10)`（3 Rx, 114 subcarrier, 10 packet）
- 读 `pose2d.npy[t-1]` → `(17, 2)`，加 neck → `(18, 2)`
- 论文 amplitude only：丢弃 CSIphase。
- **数据划分**：按 MM-Fi 官方协议 3 种 split 之一（建议 protocol 1：subject-cross，S01-S30 训练，S31-S40 测试）。先实现 random split 跑通，再切换。

### 3.2 CSI 预处理 `data/csi_preprocess.py`

**上采样到 (M=64, NR=3, NS=114)：**
- 时间维 10 → 64：`scipy.signal.resample` 或 zero-insert + 低通滤波（论文公式 (2)）。
- 频率维 **114 → 114**（保持原始分辨率，**不下采样**，避免高频细节丢失）。
- 论文用的是"插零 + 低通滤波"，但 `scipy.signal.resample`（FFT-based）效果等价且更简单。

**最终 CSI 张量**：`(M, NR, NS) = (64, 3, 114)`，dtype float32，做 per-sample 归一化（z-score 或 min-max）。

**Config 开关 `csi.subcarrier_mode`：**
- `keep`（默认）：保留 114 子载波 → 频率 token 数 = 114
- `learned64`：用 `nn.Conv1d(114, 64, 1)` 学习线性投影到 64 → 频率 token 数 = 64
- `resample64`：FFT 下采样到 64（仅做对照，不推荐）

### 3.3 GT 热图合成 `data/heatmap_gt.py`

输入 `(18, 2)` 关键点坐标（先把 [-0.8, 0.8] 归一化范围线性映射到 [0, 35]），输出：

- **PCM**：`(19, 36, 36)` —— 18 个关键点各自一张 2D 高斯热图（σ=1.5），第 19 张是前 18 张的逐像素均值（论文规范）。
- **PAF**：`(38, 36, 36)` —— 19 条肢体连接（OpenPose 18-keypoint 拓扑），每条肢体在连接线段邻域内填充单位方向向量 (vx, vy)，宽度 σ_paf ≈ 1。

**关键函数**：
- `gaussian_2d(center, size=36, sigma=1.5)` → PCM 单通道
- `paf_line(p1, p2, size=36, width=1)` → 返回 (vx_map, vy_map)

**OpenPose 18-keypoint 顺序**（与论文一致）：
`[nose, neck, R-Sh, R-El, R-Wr, L-Sh, L-El, L-Wr, R-Hip, R-Kn, R-An, L-Hip, L-Kn, L-An, R-Eye, L-Eye, R-Ear, L-Ear]`
**19 条肢体**：参考 OpenPose `BODY_25` 子集中 18-kpt 部分，写死在常量表里。

### 3.4 TFDDT 模块 `models/tfddt.py`

输入：上采样后的 `x` shape `(B, M=64, NR=3, NS=114)`（默认 `keep` 模式）。

```
# Frequency tokens：每个 subcarrier 一个 token，把 (M, NR) 展平
F = x.permute(0, 3, 1, 2).reshape(B, NS, M*NR)          # (B, 114, 192)
# Temporal tokens：每个 packet 一个 token，把 (NS, NR) 展平
T = x.permute(0, 1, 3, 2).reshape(B, M,  NS*NR)         # (B, 64, 342)
# 线性投影到 embed_dim=1296（论文值；可调小到 256/512 试验）
F = self.freq_proj(F)   # (B, 114, 1296)
T = self.time_proj(T)   # (B, 64,  1296)
# 加可学习位置 embedding（长度按各自 token 数）
F = F + self.freq_pos_embed    # (1, 114, 1296)
T = T + self.time_pos_embed    # (1,  64, 1296)
```

> 说明：论文写的"token length 1296"对应 36×36 reconstruction 的需求。直接让 raw token 维度 = M·NR = 192 也合法，但用线性层投影到 1296 更贴近论文图示。**推荐 embed_dim=1296，但允许 config 调小**以适应显存。
> 频率 token 数从 64 改成 114 不影响 attention 计算（attention 对序列长度不敏感），只影响 reconstruction 的 channel 数。

### 3.5 双路 Attention 特征提取 `models/attention_extractor.py`

每路 = 8 层标准 Transformer Encoder（论文 Table II "8 layers"）：
- `nn.LayerNorm` → `nn.MultiheadAttention(embed_dim=1296, num_heads=8)` → residual
- `LayerNorm` → 2-层 FFN（Linear→ReLU→Dropout→Linear）→ residual

两路参数独立。输出：`(B, 64, 1296)` 各自一个。

**Reconstruction Layer**（每路输出后接）：
1. `view(B, N, 36, 36)`：把 1296 reshape 成 36×36，把 N 个 token 当 N 个 channel
   - 频率路：N = 114
   - 时间路：N = 64
2. `Conv2d(N → 64, 3×3, pad=1)` + `BN` + `ReLU`（**统一压成 64 通道**，避免后续 MSFN 通道数随 NS 漂移）

最后 **concat** 两路：`(B, 128, 36, 36)` —— 这是后续 MSFN 的输入特征 Φ₀。
（`learned64` 模式下频率路本身就是 64 通道，跳过此处的通道压缩。）

### 3.6 MSFN（3 stage）`models/msfn.py`

**Heatmap Decoder**（论文 Fig.6, Table II "MultiFormer" 列）：
```
Conv 3x3, in=128, out=128, pad=1, ReLU       (×2, 即 Conv1+Conv2 共两次)
Conv 3x3, in=128, out=128, pad=1, ReLU       (×2, Conv3+Conv4)
Conv 1x1, in=128, out=512, ReLU
Conv 1x1, in=512, out=19  → PCM
Conv 1x1, in=512, out=38  → PAF
```
两个分支共享前面的卷积，最后 1×1 分头输出 PCM 和 PAF。

**PAPM**（论文 Eq. 7-8, Fig.5）：输入 `H_{i-1} = concat(PCM, PAF)` shape `(B, 57, 36, 36)`：
- **通道注意力 W_C**：`MaxPool` & `AvgPool` → 共享 MLP → sigmoid → `(B, 256, 1, 1)`（与 Φ 通道对齐，需把 Φ 升到 256 通道；或直接对 128 通道做注意力，更简洁——计划采取后者：`W_C` shape `(B, 128, 1, 1)`）。

  > 论文公式写 `W_C ∈ R^{1×256}`，但其图 Fig.5 与 reconstruction concat 后是 128，文中 "i ∈ R^{256×36×36}" 与 Table II "Conv4 out=512" 对不齐。**实现采取 128 通道方案**（与 reconstruction 输出一致），保持代码自洽，结果不会受影响。

- **空间注意力 W_S**：对 `H_{i-1}` 沿通道做 MaxPool & AvgPool → concat → `Conv2d(2→1, 7×7)` → sigmoid → `(B, 1, 36, 36)`。
- **特征融合**：`Φ_i = Φ_{i-1} ⊙ W_C ⊙ W_S`（broadcast 乘法）。

**总流程**（论文 Fig.1）：
```
Φ_0 = ExtractorOutput                             # (B, 128, 36, 36)
PCM_1, PAF_1 = Decoder_1(Φ_0)
Φ_1 = PAPM(Φ_0, [PCM_1, PAF_1])
PCM_2, PAF_2 = Decoder_2(Φ_1)
Φ_2 = PAPM(Φ_1, [PCM_2, PAF_2])
PCM_3, PAF_3 = Decoder_3(Φ_2)
return [(PCM_1,PAF_1), (PCM_2,PAF_2), (PCM_3,PAF_3)]
```
3 个 Decoder **不共享参数**（论文 Fig.1 显示 Decoder1/2/3 独立）。

### 3.7 Loss `train.py`

论文公式 (13)-(15)：所有 stage 的 PCM-MSE 与 PAF-MSE 之和。
```python
loss = sum( MSE(PCM_i, PCM_gt) + MSE(PAF_i, PAF_gt) for i in 1..3 )
```

**训练超参（论文 Table II）**：
- Optimizer：Adam
- LR: 1e-3，每 15 epoch decay × 0.7（按论文"weight decay 0.7"实为 LR decay）
- Batch size: 32
- Epochs: 100

### 3.8 Pose Decoder `decode/pose_decoder.py`（仅推理用）

1. **NMS** 在每张 PCM 通道（0..17，跳过第 19 张均值通道）上找局部极大值（3×3 邻域，阈值 0.1）→ 候选关键点集 `D_j`。
2. **PAF 线积分**（论文公式 (10)）：对每对相连的候选 `(d_m, d_n)`，沿连线均匀采样 10 个点，累加 PAF 投影到单位方向向量上的分量 → 连接置信度 `C_mn`。
3. **匈牙利匹配**（`scipy.optimize.linear_sum_assignment`）：对每种肢体类型独立求最大权匹配，得到最终骨架。

> 单人评估时可以直接 `argmax` 简化，多人评估再走完整 decoder。

### 3.9 可视化对比 `visualize.py` + `utils/viz.py`

**目标**：每个环境（E01-E04）随机抽 N 帧，每一帧输出**一张 PNG**，图内同时显示 GT 骨架和预测骨架。

**`utils/viz.py` 提供工具函数：**
```python
# 18-keypoint 骨架的连接关系（OpenPose 拓扑）
LIMBS_18 = [(0,1),(1,2),(2,3),(3,4),(1,5),(5,6),(6,7),(1,8),(8,9),(9,10),
            (1,11),(11,12),(12,13),(0,14),(14,16),(0,15),(15,17),(2,8),(5,11)]

def draw_skeleton(ax, kpts18, color, label, marker='o'):
    """在给定 axes 上画一副骨架：先画肢体连线，再画关键点。
    kpts18: (18, 2) numpy, 单位坐标系（与图像/heatmap 对齐）
    跳过坐标为 (0,0) 或 NaN 的无效点。"""

def render_compare(gt_kpts, pred_kpts, rgb=None, title='', save_path=None):
    """生成单张对比图：
    - 若提供 rgb (H,W,3)，用 1×2 subplot：左 = GT 叠图，右 = Pred 叠图
    - 否则在白色画布上用 1×2 subplot 画两副骨架
    - 标题包含环境/受试者/动作/帧号/PCK@20"""
```

**`visualize.py` 主流程：**
```python
# 1. 加载训练好的 checkpoint
model = MultiFormer(...).load_state_dict(ckpt)
model.eval()

# 2. 遍历 4 个环境
for env in ['E01', 'E02', 'E03', 'E04']:
    # 列出该 env 下所有 (subject, action, frame) 三元组
    all_samples = enumerate_env_frames(env)
    # 随机抽 N 帧（默认 N=8，可 config）
    sampled = random.sample(all_samples, k=N)
    for (sub, act, frm) in sampled:
        csi = load_csi(env, sub, act, frm)               # (3, 114, 10)
        gt2d = load_pose2d(env, sub, act)[frm]            # (17, 2) → 加 neck → (18, 2)
        rgb  = load_rgb(env, sub, act, frm)               # (H, W, 3) 可选
        with torch.no_grad():
            pcm, paf = model(preprocess(csi))[-1]         # 取 stage-3 输出
        pred2d = decode_single_person(pcm, paf)           # (18, 2)，坐标系反归一化到 pose2d 范围
        save = f'outputs/viz/{env}/{sub}_{act}_f{frm:03d}.png'
        render_compare(gt2d, pred2d, rgb=rgb,
                       title=f'{env}/{sub}/{act} frame={frm}',
                       save_path=save)
```

**关键细节：**
- 关键点坐标系对齐：`pose2d.npy` 在 [-0.8, 0.8] 范围；模型输出 PCM 是 36×36 网格上的 argmax → 需要反映射回 pose2d 坐标系再画。
- 颜色规范：GT 用绿色实线 + 圆点；Pred 用红色虚线 + 三角点。
- 若提供 RGB 背景：把 pose2d 的 [-0.8, 0.8] 映射到 RGB 的像素坐标（需要知道 MM-Fi 的归一化原点；查 MM-Fi toolkit 文档，一般是 (W/2, H/2) 中心 + 缩放）。
- 不需要 RGB 时直接画在白底 36×36 → 放大到 360×360 显示。
- 输出文件名包含 env/subject/action/frame，便于人工筛选。

### 3.10 评估 `utils/metrics.py`

**PCK@k**（论文公式 (16)）：
```
正确 ⇔ ‖pred_j − gt_j‖₂ ≤ k · √(rs² + lh²)
其中 rs = 右肩坐标，lh = 左髋坐标（torso 长度归一化）
```
论文在 MM-Fi 上用 PCK@20、@30、@40。

输出：每个关键点的 PCK + 17/18 平均 PCK。

---

## 4. 关键文件对照表（要新建/修改）

| 文件 | 行数估计 | 功能 |
|---|---|---|
| `data/mmfi_dataset.py` | ~150 | Dataset 类 + split 逻辑 |
| `data/csi_preprocess.py` | ~80 | resample + normalize |
| `data/heatmap_gt.py` | ~120 | PCM/PAF 合成（含 OpenPose 18kpt 拓扑常量） |
| `models/tfddt.py` | ~60 | Tokenization + embedding |
| `models/attention_extractor.py` | ~100 | 双路 8-layer Transformer + Reconstruction |
| `models/papm.py` | ~70 | CBAM-style 通道+空间注意力 |
| `models/heatmap_decoder.py` | ~50 | PCM/PAF 解码器 |
| `models/msfn.py` | ~50 | 3-stage 串接 |
| `models/multiformer.py` | ~40 | 顶层组装 |
| `decode/pose_decoder.py` | ~150 | NMS + PAF 积分 + 匈牙利 |
| `utils/metrics.py` | ~60 | PCK |
| `train.py` | ~150 | 训练 loop + ckpt + log |
| `eval.py` | ~80 | 加载 ckpt + 评估 |
| `visualize.py` | ~120 | 各环境随机采帧 + GT/Pred 对比图输出 |
| `utils/viz.py` | ~100 | 骨架绘制工具函数 |
| `configs/default.yaml` | ~30 | 全部超参 |

总计约 1400 行 Python。

---

## 5. 端到端验证步骤

**Step 1 — 数据 sanity check**（小脚本）：
- 加载 1 个 trial，检查 CSI shape、pose 范围、合成 PCM/PAF 的可视化（matplotlib 显示热图叠加在 mock 图像上）。

**Step 2 — 模型前向 smoke test**：
```bash
python -c "from models.multiformer import MultiFormer; import torch; m=MultiFormer(); x=torch.randn(2,64,3,64); o=m(x); print([(p.shape,a.shape) for p,a in o])"
```
预期输出 3 个 (PCM:(2,19,36,36), PAF:(2,38,36,36))。

**Step 3 — overfit 单 batch**：
- 取 1 个 batch（32 sample），训练 200 step，loss 应降到接近 0；可视化 PCM 应清晰对应关键点位置。如果不能 overfit，模型有 bug。

**Step 4 — 完整训练**（先用 protocol 1 子集，比如 E01 全部 + S01-S20）：
- 100 epoch，监控 train/val loss、val PCK@20。
- 论文 MM-Fi 的 MultiFormer 全模型 PCK@20 = 0.7225，可作为目标参考。

**Step 5 — 完整评估 + 对照表**：
- 实现 `eval.py`，输出 17 关键点的 PCK@20/30/40，与论文 Table VIII 对比。

**Step 6 — 可视化对比**（必做）：
- 运行 `python visualize.py --ckpt outputs/best.pth --n_per_env 8`
- 输出 `outputs/viz/E0{1..4}/*.png`，每张图含 GT(绿) + Pred(红) 骨架并标注 PCK@20。
- 抽查 32 张图人工目检：手腕/脚踝是否合理，多帧之间预测是否稳定。


## 6. 执行顺序（建议）

1. ✅ 写好 `configs/default.yaml`、`requirements.txt`
2. ✅ `data/heatmap_gt.py` + 可视化测试
3. ✅ `data/csi_preprocess.py` + `data/mmfi_dataset.py` + DataLoader 测试
4. ✅ 自底向上写模型：`tfddt.py` → `attention_extractor.py` → `papm.py` → `heatmap_decoder.py` → `msfn.py` → `multiformer.py`
5. ✅ smoke test + 单 batch overfit
6. ✅ `train.py` 完整训练 pipeline
7. ✅ `decode/pose_decoder.py` + `utils/metrics.py` + `eval.py`
8. ✅ 跑实验、出 PCK 表、与论文对照
9. ✅ `utils/viz.py` + `visualize.py` 输出每环境对比图
